apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thronesdb-php.fullname" . }}-test-connection"
  labels:
    {{- include "thronesdb-php.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thronesdb-php.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
